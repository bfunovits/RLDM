---
phase: 01-repository-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - /media/bernd/nvme/r_projects/acad_RLDM/
autonomous: false

must_haves:
  truths:
    - "Root directory contains only essential files"
    - "Repository structure is clean and organized"
  artifacts:
    - path: "/media/bernd/nvme/r_projects/acad_RLDM/"
      provides: "Clean root directory"
      file_patterns:
        present: ["DESCRIPTION", "NAMESPACE", "README.md", "LICENSE.md", "CLAUDE.md", ".gitignore", ".Rbuildignore"]
        absent: ["benchmark_particle_filters.R", "log_claude.md", "SMC_*.md", "debug_*.R", "test_*.R"]
    - path: "/media/bernd/nvme/r_projects/acad_RLDM/inst/benchmarks/"
      provides: "Benchmark scripts location"
      file_count: "≥1 file"
    - path: "/media/bernd/nvme/r_projects/acad_RLDM/logs/"
      provides: "Development logs directory"
      subdirs: ["claude", "smc", "debug", "test_scripts"]
  key_links:
    - from: "clean root"
      to: "phase success"
      via: "only essential files remain"
      pattern: "ls -la shows DESCRIPTION, NAMESPACE, README.md, LICENSE.md, CLAUDE.md, .gitignore, .Rbuildignore as main files"
    - from: ".gitignore"
      to: "ignored development artifacts"
      via: "logs/ directory excluded from git"
      pattern: "git status shows logs/ as untracked"
---

<objective>
Final cleanup and verification of repository structure.

Purpose: Complete Phase 1 by cleaning up remaining root directory files and verifying the organized structure meets success criteria.
Output: Clean root directory with only essential files, organized development artifacts, human verification of structure.
</objective>

<execution_context>
@/home/bernd/.claude/get-shit-done/workflows/execute-plan.md
@/home/bernd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-foundation/01-RESEARCH.md
@.planning/phases/01-repository-foundation/01-01-SUMMARY.md
@.planning/phases/01-repository-foundation/01-02-SUMMARY.md

# From previous plans:
- Plan 01-01: Created directory structure, LICENSE.md, updated .gitignore/.Rbuildignore
- Plan 01-02: Moved benchmark scripts, log files, debug/test scripts to organized locations

# Current state:
- Directories exist: logs/{claude,smc,debug,test_scripts}, inst/benchmarks/
- Configuration files updated: .gitignore, .Rbuildignore, LICENSE.md
- Most development files moved from root to organized locations
- Need final cleanup of root directory and verification
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up remaining root directory files</name>
  <files>/media/bernd/nvme/r_projects/acad_RLDM/</files>
  <action>
    Clean up remaining non-essential files in root directory.

    Based on analysis of current root directory and Phase 1 success criteria:

    1. **Essential files that MUST stay in root:**
       - DESCRIPTION, NAMESPACE, README.md, LICENSE.md, CLAUDE.md
       - .gitignore, .Rbuildignore
       - RLDM.Rproj (RStudio project file)
       - _pkgdown.yml (pkgdown config)
       - CONTRIBUTING.md (could stay or move to .github/)

    2. **Files to review and potentially clean up:**
       - .DS_Store (macOS file) - delete if exists (should be in .gitignore)
       - Rplots.pdf (generated plot) - delete
       - .Rhistory (R history) - keep (in .gitignore)
       - RLDM.code-workspace (VSCode workspace) - keep
       - Various .R files that might be utility scripts (check_kf.R, compare_covariances.R, etc.) - evaluate

    3. **Action steps:**
       a. Delete temporary files:
          rm -f /media/bernd/nvme/r_projects/acad_RLDM/.DS_Store 2>/dev/null || true
          rm -f /media/bernd/nvme/r_projects/acad_RLDM/Rplots.pdf 2>/dev/null || true

       b. Review remaining .R files:
          List remaining .R files in root after Plan 01-02 moves
          Determine if they're utility scripts (keep) or development artifacts (move to logs/)

       c. Ensure no stray debug_*, test_*, SMC_*, log_* files remain

    4. **Final root should have:**
       - Essential package files (DESCRIPTION, NAMESPACE, etc.)
       - Configuration files (.gitignore, .Rbuildignore)
       - Project files (RLDM.Rproj, _pkgdown.yml)
       - Minimal utility scripts if essential

    Use ls -la to verify clean root.
  </action>
  <verify>
    Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/
    Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/*.R 2>/dev/null | wc -l
    Check: Root directory shows clean structure, minimal .R files
  </verify>
  <done>
    Root directory cleaned up, only essential files remain.
    Temporary files (.DS_Store, Rplots.pdf) deleted.
    Remaining .R files are utility scripts, not development artifacts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify repository structure</name>
  <what-built>Complete Phase 1 repository foundation with organized structure</what-built>
  <how-to-verify>
    Please verify the repository structure meets Phase 1 success criteria:

    1. **Check root directory contains only essential files:**
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/
       - Should see: DESCRIPTION, NAMESPACE, README.md, LICENSE.md, CLAUDE.md, .gitignore, .Rbuildignore
       - Should NOT see: benchmark_particle_filters.R, log_claude.md, SMC_*.md, many debug_*.R, test_*.R

    2. **Check benchmark scripts location:**
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/inst/benchmarks/
       - Should see: benchmark_particle_filters.R

    3. **Check log files organization:**
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/logs/claude/
       - Should see: log_claude.md
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/logs/smc/
       - Should see: SMC_EXTENSION_LOG.md, SMC_LOG_20260123_2358.md, etc.
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/logs/debug/
       - Should see: OPTIMAL_BIAS_DEBUG_SUMMARY.md, debug_*.R files
       - Run: ls -la /media/bernd/nvme/r_projects/acad_RLDM/logs/test_scripts/
       - Should see: test_*.R files

    4. **Check git ignore patterns:**
       - Run: git status
       - Should see: logs/ directory as untracked (properly ignored)
       - Should NOT see: build artifacts (.tar.gz, .Rcheck/) in changes

    5. **Verify overall structure:**
       - Root is clean and organized
       - Development artifacts are in appropriate directories
       - Configuration files are comprehensive
  </how-to-verify>
  <resume-signal>Type "verified" if structure looks good, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Root directory inspection shows clean structure
2. All moved files are in correct organized locations
3. .gitignore properly excludes development directories
4. .Rbuildignore excludes non-package files from build
5. LICENSE.md exists with proper content
</verification>

<success_criteria>
- [ ] Root directory contains only essential files (README.md, LICENSE.md, CLAUDE.md, .gitignore)
- [ ] Benchmark scripts are moved to inst/benchmarks directory
- [ ] Log files are moved to logs/ directory
- [ ] Proper .gitignore for R package development is established
- [ ] Human verification confirms structure is correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-foundation/01-03-SUMMARY.md`
</output>
